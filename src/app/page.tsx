'use client';

export default function HomePage() {
  return (
    <main style={{ minHeight: '70vh', display: 'grid', placeItems: 'center' }}>
      <div style={{ textAlign: 'center' }}>
        <h1>M2 Auth & Profiles</h1>
        <p>Baseline boilerplate is running.</p>
      </div>
    </main>
  );
}
